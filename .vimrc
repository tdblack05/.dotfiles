syntax on
set ruler
set autoindent
set number
set ignorecase
color elflord
	"Timothy Black"
	"In this guided lab, I used a couple different commands. First I opened the terminal, and then I used 'pwd' to look at the global path of my home directory. After that, I created a VIM file using 'vim .vimrc' and then added text. Once that was done, I renamed the file, and typed this response."  
